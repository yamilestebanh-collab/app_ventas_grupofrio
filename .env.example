# kold-field — ejemplo de variables de entorno (NO committear el .env real)
#
# BLD-20260405-022 (Fase 1 fast-track) — service user compartido kold_field_svc.
# Copia este archivo a `.env` y llena el valor antes de `expo start` o builds
# locales. Para EAS Build en la nube, usar `eas secret:create` en vez de .env.

# API key del service user kold_field_svc (uid=21 en grupofrio.odoo.com).
# Scope `rpc`, sin expiración. NUNCA loguear este valor ni exponerlo a UI.
# Rotación: ver BLD-20260405-022_sebastian_backend_specs.md seccion 3.
GF_SVC_API_KEY=paste_here
